import styles from "./OrdersList.module.scss";
import { OrderType } from "./types";

const Order = ({ order }: {
    order: OrderType
}) => {
    return (
        <div>Привет</div>
    )
}

export default Order;